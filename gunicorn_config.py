# Listen on all interfaces, port 8000 (replace with desired values)
bind = ("0.0.0.0:8000")
# Number of worker processes
workers = 2
# Number of threads per worker
threads = 4
# Logging level (options: debug, info, warning, error, critical)
log_level = "info"
